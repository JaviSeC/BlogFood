// function searchRecipe() {
//   alert("Boton funciona") 
// }



// document.getElementById('button-search-recipe').addEventListener('submit', function (e) {



  // e.preventDefault(); // Evita el envío del formulario
  // var searchTerm = document.getElementById('search-input').value.toLowerCase();
  // var resultsContainer = document.getElementById('search-results');
  // resultsContainer.innerHTML = ''; // Limpia los resultados anteriores
  // var article = document.getElementsByTagName('article');
  // var matchingParagraphs = [];

  // for (var i = 0; i < article.length; i++) {
  //   var articleText = article[i].textContent.toLowerCase();
  //   if (articleText.includes(searchTerm)) {
  //     matchingArticle.push(article[i]);
  //   }
  // }

  // if (matchingArticles.length === 0) {
  //   resultsContainer.innerHTML = 'No se encontraron resultados.';
  // } else {
  //   for (var j = 0; j < matchingArticle.length; j++) {
  //     var result = document.createElement('div');
  //     result.className = 'result';
  //     result.textContent = matchingArticle[j].textContent;
  //     resultsContainer.appendChild(result);
  //   }
  // }
// });




// document.getElementById('my-form').addEventListener('submit', function (e) {
//   e.preventDefault(); // Evita el comportamiento predeterminado de reinicio del formulario
//   // Aquí puedes realizar acciones adicionales, como enviar los datos del formulario a través de Ajax
//   // Ejemplo: Envío de datos a través de Ajax utilizando la biblioteca Axios
//   var formData = new FormData(this);
//   axios.post('procesar.php', formData)
//     .then(function (response) {
//       // Manipular la respuesta de la subpágina aquí
//       console.log(response.data);
//     })
//     .catch(function (error) {
//       // Manejar errores de la solicitud
//       console.error(error);
//     });
// });








